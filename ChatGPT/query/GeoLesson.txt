Create a brief daily geography lesson with two parts: one U.S. state and one sovereign country other than the United States. Use ChatGPT/rotation/geography-rotation.json and ChatGPT/rotation/geography-audit.jsonl as the authoritative no-repeat ledgers.

For the state rotation, use a randomly shuffled permutation containing each of the 50 U.S. states exactly once. Use one state per calendar day in order without repetition. After all 50 have been used, validate exact membership and uniqueness, increment the cycle number, create a newly shuffled complete permutation, and begin at position one. Avoid making the first state of a new cycle equal to the last state of the prior cycle when possible.

Maintain a parallel country cycle using the canonical list of 194 sovereign countries: the 193 United Nations member states plus the Holy See and the State of Palestine, excluding the United States. Shuffle the complete list, use one country per calendar day without repetition, validate the completed cycle, and begin a newly shuffled cycle on day 195. Avoid an immediate repeat across the cycle boundary when possible.

Validate each stored permutation for exact membership and uniqueness before using it. On the first scheduled run, initialize both cycles beginning on that run date. If today's date already has an audit record, reuse the same state and country and do not advance either cycle again. Persist cycle numbers, shuffled orders, next positions, last-used values, and initialization date. Append exactly one audit record per date containing the date, state, state cycle and position, country, and country cycle and position. Never rewrite prior audit facts except to repair a demonstrably malformed duplicate safely.

For each selection, explain useful physical geography such as location, borders, major rivers, lakes, mountains, ranges, oceans, landforms, climate, and how geography affects settlement or economic life. Keep the state section and country section each under about 1,500 characters. Write clean plain text for text-to-speech without Markdown decoration, bullets, or tables.

Start with this pattern: Monday May 25, 2026 Geography Today by ChatGPT. Do not repeat the date at the start of the country section.
